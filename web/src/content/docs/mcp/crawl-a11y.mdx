---
title: Crawl & A11y
description: Explore and audit.
---

`browser_crawl` BFS up to `maxDepth`/`maxPages`, same-origin, asset noise filtered, `sitemap.xml` discovery, real HTTP status from telemetry.

`browser_audit_a11y` runs axe-core WCAG 2.1 AA (tags wcag2a/aa, wcag21a/aa) → Markdown summary with impact and helpUrl.
