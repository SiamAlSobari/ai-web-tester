---
title: Scenario Runner
description: YAML replay + data-driven + mocks.
---

Features: `beforeAll`/`afterAll`, `retries`, `concurrency` (≤4), `{{var}}` interpolation + `{{faker.email|uuid|int:1-100|name}}`, `mock`/`mockReset` via `routeMock`, `waitFor`, `extract` chaining.

Run: `ai-test run ./scenario.yaml` or via MCP `browser_run_scenario`.

Reports per row: `test-reports/<name>-rowN.md`.
